import React from "react";
// import PropTypes from 'prop-types'

const Contacts = () => {
  return <div>Contacts You haven't added any bootcamp</div>;
};

// Contacts.propTypes = {}

export default Contacts;
